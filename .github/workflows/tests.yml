name: tests

on: [ push ]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: run tests
        run: |
          pip install -r requirements.txt
          pytest